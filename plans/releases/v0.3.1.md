- forge pr -h (replace fully with conv, allow the conv -h text to explain)
- forge pr create should auto fill --title and --base just as it does --head to the commit message and the current base (default to gits default main branch name if thats not specific enough)
- research (not necessarily in scope for forge-cli): add tooling to make it easier to list branches locally in order of creation / commit dependency 
- add batched pr create command that submits PR's to merge with --base (using their commit messages as titles, or should it be their branch names?)

