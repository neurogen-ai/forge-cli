features: 

- Add a protocol to config so that http also works (add warninng if not connecting to localhost)
- Only fire the diagnose logic after failure, proceed naively (and quickly) until there is a reason to diagnose, on failure of diagnosis, present 'failed to diagnose'

- The default cache should move to .local/share or .local/state, at most it should be in .forge not ~/forge-cache
